doctrine:
    dbal:
        default_connection: 'default'
        connections:
            default:
                driver: 'pdo_mysql'
                url: "sqlite:///%kernel.project_dir%/data.db"
    orm:
        auto_generate_proxy_classes: false
        auto_mapping: true
