httplug:
    plugins:
        logger: ~
    clients:
        oauth2:
            factory: 'httplug.factory.guzzle6'
            plugins: ['httplug.plugin.logger']
            config:
                verify: false
                timeout: 2
