imports:
    - { resource: issuer_discovery.yml }

framework:
    test: ~
    secret: 'test'
    default_locale:  'en'
    router:
        resource: '%kernel.root_dir%/config/routing.yml'
        strict_requirements: ~
    http_method_override: true
